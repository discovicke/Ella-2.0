services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: net25
      POSTGRES_PASSWORD: SecretNet25Password!
      POSTGRES_DB: net25_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
